spring:
    datasource:
        url: jdbc:postgresql://aws-0-sa-east-1.pooler.supabase.com:6543/postgres?user=postgres.nmcgdztcymerhtkgdots&password=XdF37GAA4P8seyyM&prepareThreshold=0
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
    servlet:
        multipart:
            max-file-size: -1
            max-request-size: -1
            enabled: true
aws:
    s3:
        bucket-name: ${SUPABASE_BUCKET_NAME:helpline-storage}
api:
    security:
        token:
            secret: ${JWT_SECRET:my-secret}
springdoc:
    swagger-ui:
        path: "/docs"
